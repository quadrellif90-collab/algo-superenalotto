# SuperEnalotto — dipendenze
# Core runtime: solo stdlib (sqlite3, csv, json, secrets, urllib, http.server)
# Dipendenze opzionali per GUI desktop / build — installare solo se necessario.

# Desktop GUI (launcher.py + gateway/desktop.py)
pywebview>=4.4
pystray>=0.19
Pillow>=10.0

# Build / packaging
pyinstaller>=6.0
pyinstaller-hooks-contrib>=2024.0

# Dev / test (opzionale)
# pytest
# ruff
# playwright  # solo per test E2E .playwright-mcp
